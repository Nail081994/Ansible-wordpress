---

 - name: Install Wordpress on your machine
   hosts: all
   gather_facts: yes
   become: yes
   become_method: sudo

   tasks:
           - name: Configure Wordpress
             import_role:
                     name: ansible-wordpress
